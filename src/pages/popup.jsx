import React from 'react';
import ReactDOM from 'react-dom/client';
import PasswordManager from '../App';
import '../styles/tailwind.css';

ReactDOM.createRoot(document.getElementById('root')).render(<PasswordManager />);
